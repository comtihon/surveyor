sudo: required
language: python
 - "3.6"
cache:
  directories:
    - $HOME/services
services:
  - docker
script:
  - make run
  - python -m pip install --upgrade pip
  - bash travis/sleep_if_needed.sh
  - make test_run
