sudo: required
language: python
 - "3.6"
cache:
  directories:
    - $HOME/services
services:
  - docker
script:
  - bash travis/install_deps.sh
  - make run
  - bash travis/sleep_if_needed.sh
  - make test_run
